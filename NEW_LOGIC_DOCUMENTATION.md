"""
НОВАЯ ЛОГИКА ОБРАБОТКИ ИНСТРУМЕНТОВ В AIRI
============================================

Главное правило: ОЗВУЧИВАТЬ ТОЛЬКО РЕЗУЛЬТАТЫ ФУНКЦИИ text_to_audio
"""

# ============ СТАРАЯ ЛОГИКА (ДО) ============
OLD_LOGIC = """
1. ИИ отправляет ответ "В Москве 20 градусов"
2. ВСЕГДА озвучиваем этот текст (неважно, вызвала ли ИИ функцию)
3. Если были tool_calls - выполняем их

ПРОБЛЕМА: Озвучиваем не только результаты text_to_audio функции,
          но и обычные текстовые ответы ИИ
"""


# ============ НОВАЯ ЛОГИКА (ПОСЛЕ) ============
NEW_LOGIC = """
ПРАВИЛО: Озвучивать ТОЛЬКО то, что вызвано функцией text_to_audio

Шаг 1: Получить ответ от ИИ
       ↓
Шаг 2: Проверить наличие tool_calls
       ├─ НЕТ tool_calls? → 🤐 Молчим и ждем (ничего не озвучиваем)
       │
       └─ ЕСТЬ tool_calls? → Переходим на Шаг 3

Шаг 3: Разделить function на две группы
       ├─ text_to_audio → Выполнить СРАЗУ
       │  └→ Результат добавить в очередь озвучки 🔊
       │
       └─ Остальные функции → Выполнить ПАРАЛЛЕЛЬНО
          └→ Результаты сохранить в истории

Шаг 4: Если были остальные функции
       ├─ Отправить результаты обратно в ИИ 📤
       ├─ Получить новый ответ от ИИ
       ├─ Проверить новый ответ на text_to_audio
       └─ Если есть → озвучить 🔊

Шаг 5: Сохранить всё в историю
"""


# ============ ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ ============

EXAMPLE_1 = {
    "название": "Обычный ответ без функций",
    "запрос": "Привет",
    "ответ_ии": "Привет! Как дела?",
    "tool_calls": [],
    "результат": "🤐 Молчим и ждем (никакой озвучки)"
}

EXAMPLE_2 = {
    "название": "Только озвучка",
    "запрос": "Расскажи шутку",
    "ответ_ии": "Вот шутка: ...",
    "tool_calls": [
        {
            "function": "text_to_audio",
            "args": {"text": "Вот шутка по-русски"}
        }
    ],
    "результат": "🔊 Озвучиваем результат text_to_audio"
}

EXAMPLE_3 = {
    "название": "Функция + озвучка",
    "запрос": "Какая погода?",
    "ответ_ии": "Сейчас проверю",
    "tool_calls": [
        {
            "function": "get_weather",
            "args": {"location": "Москва"}
        }
    ],
    "обработка": [
        "1. Выполняем get_weather",
        "2. Получаем результат: {temperature: 20, condition: облачно}",
        "3. Отправляем результат обратно в ИИ",
        "4. ИИ может ответить: 'В Москве 20 градусов'",
        "5. Если ИИ вызовет text_to_audio - озвучиваем"
    ],
    "результат": "⚙️ Выполнить функцию → 📤 Запрос в ИИ → 🔊 Озвучка если нужна"
}

EXAMPLE_4 = {
    "название": "Смешанный случай",
    "запрос": "Включи музыку и скажи время",
    "ответ_ии": "Включаю и озвучиваю...",
    "tool_calls": [
        {
            "function": "text_to_audio",
            "args": {"text": "Сейчас десять часов"}
        },
        {
            "function": "get_current_time",
            "args": {}
        }
    ],
    "обработка": [
        "1. Разделяем: text_to_audio И get_current_time",
        "2. Озвучиваем text_to_audio СРАЗУ",
        "3. Выполняем get_current_time параллельно",
        "4. Результаты сочетаемся в истории",
        "5. ИИ получит контекст и может ответить ещё"
    ],
    "результат": "🔊 Озвучка + ⚙️ Функция параллельно"
}


# ============ АРХИТЕКТУРА ПОТОКОВ ============

THREAD_ARCHITECTURE = """
┌─────────────────────────────────────────────────────────────┐
│ thread_record_voice                                         │
│ 🎤 Слушает микрофон                                         │
│ → Распознаёт текст (STT)                                    │
│ → Добавляет в voice_input_queue                             │
└──────────────────┬──────────────────────────────────────────┘
                   │
                   ↓ voice_input_queue
┌──────────────────────────────────────────────────────────────┐
│ thread_process_ai                                            │
│ 🤖 Обработка ИИ                                              │
│                                                              │
│ 1. Получить текст из очереди                                │
│ 2. Отправить запрос в ИИ                                    │
│ 3. Проверить tool_calls:                                    │
│    ├─ text_to_audio? → Озвучить СРАЗУ                      │
│    └─ Другие? → Выполнить + Отправить результат в ИИ       │
│ 4. Сохранить всё в История                                  │
└──────────────────┬──────────────────────────────────────────┘
                   │
             ⏸️ МОЛЧИТ если БЕЗ озвучки
                   │
                   ↓ ai_response_queue (ТОЛЬКО audio результаты)
┌──────────────────────────────────────────────────────────────┐
│ thread_play_response                                         │
│ 🔊 Воспроизведение аудио                                     │
│ → Получает результаты text_to_audio                         │
│ → Синтезирует в аудиопоток (если нужно)                     │
│ → Воспроизводит                                             │
└──────────────────────────────────────────────────────────────┘
"""


# ============ КЛЮЧЕВЫЕ ИЗМЕНЕНИЯ В КОДЕ ============

KEY_CHANGES = """
1. tools/instructions.py
   ✅ Переформатирован в стандартный формат OpenAI API
   ✅ Добавлены все инструменты: search_web, get_weather, get_current_time, text_to_audio
   ✅ Обновлен SYSTEM_PROMPT с инструкциями
   
2. app/LLM/openrouter_handler.py
   ✅ Импортирует TOOLS_USE из instructions
   ✅ Передает tools в API payload
   
3. app/tools/tool_executor.py (НОВЫЙ ФАЙЛ)
   ✅ ToolCallParser - парсит tool_calls из OpenAI API формата
   ✅ ToolExecutor - выполняет functions параллельно
   ✅ extract_text_for_tts - извлекает текст для озвучки
   
4. app/main.py (thread_process_ai)
   ✅ Добавлена логика разделения functions
   ✅ text_to_audio выполняется СРАЗУ
   ✅ Остальные functions выполняются и результаты отправляются обратно в ИИ
   ✅ МОЛЧАНИЕ если нет tool_calls (никакая озвучка)
   ✅ Озвучивание ТОЛЬКО результатов text_to_audio
"""


if __name__ == "__main__":
    import json
    
    print("=" * 70)
    print("НОВАЯ ЛОГИКА ОБРАБОТКИ ИНСТРУМЕНТОВ В AIRI 2.0")
    print("=" * 70)
    
    print("\n📋 ВХОДНАЯ АРХИТЕКТУРА:")
    print(THREAD_ARCHITECTURE)
    
    print("\n✨ ПРИМЕРЫ СЦЕНАРИЕВ:")
    for i, example in enumerate([EXAMPLE_1, EXAMPLE_2, EXAMPLE_3, EXAMPLE_4], 1):
        print(f"\n{i}. {example['название']}")
        print(f"   Запрос: {example['запрос']}")
        print(f"   Результат: {example['результат']}")
    
    print("\n✅ КЛЮЧЕВЫЕ ИЗМЕНЕНИЯ:")
    print(KEY_CHANGES)
