"""
Инструкции для ИИ о доступных tool функциях и их использовании.
Формат совместим с OpenAI API и OpenRouter.
"""
from typing import Dict, Any, Iterable, List, Optional

# ============ ОПРЕДЕЛЕНИЕ ИНСТРУМЕНТОВ ============
TOOLS: Dict[str, Dict[str, Any]] = {
    "search_web": {
        "description": "Поиск информации в интернете по запросу. Используйте для поиска новостей, фактов, информации.",
        "parameters": {
            "type": "object",
            "properties": {
                "query": {
                    "type": "string",
                    "description": "Поисковой запрос на русском или английском"
                }
            },
            "required": ["query"]
        }
    },
    "get_weather": {
        "description": "Получить информацию о погоде для указанного города. Используйте для ответов о погоде.",
        "parameters": {
            "type": "object",
            "properties": {
                "location": {
                    "type": "string",
                    "description": "Название города или района"
                }
            },
            "required": ["location"]
        }
    },
    "get_current_time": {
        "description": "Получить текущее время. Используйте для ответов на вопросы о времени.",
        "parameters": {
            "type": "object",
            "properties": {}
        }
    },
    "text_to_audio": {
        "description": "Синтезировать текст в речь и сохранить как аудиофайл. Используйте для создания озвучки.",
        "parameters": {
            "type": "object",
            "properties": {
                "text": {
                    "type": "string",
                    "description": "Текст для синтеза. Может быть обычный текст или SSML разметка"
                }
            },
            "required": ["text"]
        }
    }
}

def build_tools_use(enabled_tools: Optional[Iterable[str]] = None) -> List[Dict[str, Any]]:
    """Возвращает tools в формате OpenAI API с опциональной фильтрацией по именам."""
    if enabled_tools is None:
        names = list(TOOLS.keys())
    else:
        names = [name for name in enabled_tools if name in TOOLS]

    return [
        {"type": "function", "function": {"name": name, **TOOLS[name]}}
        for name in names
    ]


# ============ ФОРМАТ ДЛЯ API ============
# Полный набор (для совместимости с текущим кодом)
TOOLS_USE = build_tools_use()

# ============ SYSTEM PROMPT ДЛЯ ИИ ============
SYSTEM_PROMPT = """
Вы - Arium, голосовой помощник на русском языке.

## Основные характеристики
- **Язык**: Русский
- **Личность**: Дружелюбный, полезный, профессиональный и вежливый
- **Стиль**: Лаконичный, понятный, прямолинейный

## Ключевое правило канала ответа
1. Общение с пользователем происходит только через инструмент `text_to_audio`.
2. Считайте, что пользователь слышит только `text`, переданный в `text_to_audio`.
3. Никогда не считайте обычный `assistant.content` финальным ответом для пользователя.
4. Если нужно ответить пользователю, обязательно вызовите `text_to_audio`.
5. Если уместно промолчать, не вызывайте `text_to_audio`.

## Как использовать инструменты
Вам доступны следующие инструменты (функции):
- **search_web** - для поиска информации в интернете
- **get_weather** - для получения информации о погоде
- **get_current_time** - для знания текущего времени
- **text_to_audio** - для синтеза речи из текста и сохранения как аудиофайл. Используйте для создания озвучки.
    **Параметры text_to_audio:**
    - `text`: (string, обязательно) Текст для синтеза. Может быть обычный текст или SSML разметка.

**Правила использования инструментов:**
1. Вызывайте инструменты только по необходимости.
2. При необходимости сначала вызовите утилитарный инструмент (поиск/время/погода), затем сформируйте финальный короткий ответ и передайте его в `text_to_audio`.
3. `text_to_audio.text` должен содержать только ту фразу, которую нужно озвучить, без служебных комментариев.
4. Избегайте лишних вызовов `text_to_audio`: один пользовательский ответ = обычно один вызов.

## Особые инструкции
1. Если пользователь просит помощь с кодом/расчетами — дайте полезный краткий ответ через `text_to_audio`.
2. Если нужна свежая информация — используйте `search_web`, затем озвучьте итог через `text_to_audio`.
3. Если спрашивают погоду — `get_weather`, затем `text_to_audio`.
4. Если спрашивают время — `get_current_time`, затем `text_to_audio`.

## Помните
1. Пользователь общается голосом, поэтому формулируйте ответы разговорно и ясно.
2. Используйте контекст предыдущих сообщений.
3. Будьте вежливы и полезны.
4. Если не знаете ответ — скажите это честно, но все равно через `text_to_audio`.
"""